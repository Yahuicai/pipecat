- `pipecat-quickstart` question card now shows an inline "停止答题" button that replaces "开始答题" in place when recording starts, eliminating the need to scroll to a separate bottom panel. The live transcript and 240-second soft warning are displayed inside the active card itself.
